{
	"name": "math.multiply",
	"type": "object",
	"description": "Returns the sum of all multiplicand values times the sum of all multiplier values or the given multiplier.",
	"inputs": {
		"multiplicand": {
			"type": "math.number"
		},
		"multiplier": {
			"type": "math.number"
		}
	},
	"outputs": {
		"result": {
			"type": "math.number"
		}
	}
}
